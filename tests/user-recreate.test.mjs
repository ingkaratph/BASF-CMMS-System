import test from'node:test';import assert from'node:assert/strict';import{prepareUser,prepareUserDeletion,verifyPassword}from'../server/users.mjs';
test('Deleted username can be recreated with new identity; active duplicate still blocked',()=>{const admin=prepareUser([],null,{username:'admin',displayName:'Admin',role:'ADMINISTRATOR',password:'admin1'},'seed');const old=prepareUser([admin],null,{username:'teama',displayName:'Team A',role:'TECHNICIAN',password:'old123'},admin.id);const deleted=prepareUserDeletion([admin,old],old.id,admin.id,old.version);assert.notEqual(deleted.username,'teama');assert.equal(deleted.id,old.id);assert(deleted.username.length<=50);const fresh=prepareUser([admin,deleted],null,{username:'teama',displayName:'Team A',role:'TECHNICIAN',password:'new123'},admin.id);assert.notEqual(fresh.id,old.id);assert(verifyPassword('new123',fresh.passwordHash));assert.throws(()=>prepareUser([admin,deleted,fresh],null,{username:'teama',displayName:'Duplicate',role:'TECHNICIAN',password:'abc123'},admin.id))});
