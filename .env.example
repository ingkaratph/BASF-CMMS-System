CMMS_API_BASE_URL=http://pd.local:1880
CMMS_API_KEY=
# Local preview defaults to loopback. Set 0.0.0.0 for LAN access after configuring login.
HOST=127.0.0.1
PORT=3000
# Required to allow LAN access. Use a long random password.
CMMS_APP_PASSWORD=
